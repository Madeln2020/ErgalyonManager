# ===========================================================================
# EDM v2 — Development / testing dependencies
# Εγκατάσταση: pip install -r requirements.txt -r requirements-dev.txt
# ===========================================================================
-r requirements.txt

pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
httpx>=0.27            # FastAPI TestClient / async tests
ruff>=0.4             # linter + formatter
mypy>=1.10            # static type checking
pre-commit>=3.7
faker>=25.0           # test data
