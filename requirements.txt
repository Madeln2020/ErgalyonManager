# ===========================================================================
# Ergalyon Data Manager (EDM v2) — Python dependencies
# Παράγεται με βάση το docs/docs_02_system_architecture.md
# Συνιστάται pinning των ακριβών εκδόσεων μετά το πρώτο `pip freeze`.
# ===========================================================================

# --- Web App / API (FastAPI) ---
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9        # uploads (PDF/XML/Excel/images)

# --- Auth / RBAC / Security ---
python-jose[cryptography]>=3.3
passlib[bcrypt]>=1.7

# --- Core Data Store (PostgreSQL + pgvector) ---
sqlalchemy>=2.0
alembic>=1.13                  # migrations & parser/schema versioning
psycopg[binary]>=3.1
pgvector>=0.2.5                # embeddings (RAG / semantic search)

# --- Job Queue (Redis + worker) ---
redis>=5.0
celery>=5.4

# --- Object Storage (MinIO / S3-compatible) ---
minio>=7.2
boto3>=1.34

# --- Parsing & Extraction ---
lxml>=5.2                      # XML structured parsing
openpyxl>=3.1                  # Excel price lists
pandas>=2.2                    # tabular normalization
camelot-py[cv]>=0.11           # PDF table extraction
pdfplumber>=0.11               # native PDF text/tables
pytesseract>=0.3.10            # OCR
Pillow>=10.3                   # image preprocessing (deskew/contrast)
opencv-python-headless>=4.9

# --- AI Orchestration (Hermes routing) / RAG ---
httpx>=0.27                    # provider calls (local / freeLLMAPI / Abacus)
tiktoken>=0.7
sentence-transformers>=3.0     # local embeddings
crawl4ai>=0.3                  # web scraping (last-resort enrichment)

# --- Observability ---
structlog>=24.1                # structured JSON logging

# --- Utilities ---
python-dotenv>=1.0
tenacity>=8.3                  # retries / fallbacks
email-validator>=2.0.0
asyncpg>=0.29.0
